# --- Injected automatically by Foundry hosting at runtime (set these for LOCAL runs) ---
# The Foundry project endpoint and model deployment name.
FOUNDRY_PROJECT_ENDPOINT=https://<account>.services.ai.azure.com/api/projects/<project>
AZURE_AI_MODEL_DEPLOYMENT_NAME=gpt-4o
# Provided by Foundry for telemetry to Application Insights (optional locally).
# APPLICATIONINSIGHTS_CONNECTION_STRING=

# --- OBO variant notes ---
# This agent acts on behalf of the signed-in user. The user's delegated token for the
# Agent 365 Tools (Mail MCP) resource is supplied per invocation (body `mail_token` or
# Authorization bearer), NOT via env. See README.md.
