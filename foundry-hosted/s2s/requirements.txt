# Foundry Hosted Agent runtime dependencies (S2S / agent-identity variant)

# Foundry chat client (agent_framework.foundry.FoundryChatClient) + Agent Framework core
agent-framework-foundry

# Foundry hosting integration (ResponsesHostServer).
# >=1.0.0a260630 => Foundry hosted-agent protocol 2.0.0 (matches azure.yaml).
agent-framework-foundry-hosting>=1.0.0a260630

# Auth + HTTP
azure-identity
httpx>=0.24.0

# Observability (Phase 2): A365 span enrichment + Azure Monitor export + the A365 observability exporter,
# which is ENABLED in main.py (a365_enable_observability_exporter=True). Pinned to the same versions the
# FH-DW sample ships. The exporter needs the Agent365.Observability.OtelWrite app role on the agent
# identity SP -- see foundry-hosted/scripts/assign-observability-role.ps1 (granted at/after deploy).
microsoft-opentelemetry>=0.1.0a3
azure-monitor-opentelemetry>=1.6.0

# Local dev
python-dotenv
