# Extra dependencies for the MCS (Copilot Studio) send path — see send_prompts_mcs.py.
# Kept separate from requirements.txt so the base engine stays lightweight (msal + requests only).
msal
microsoft-agents-copilotstudio-client
